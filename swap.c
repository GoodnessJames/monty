
#include "monty.h"

/**
 * swap - exchanges the value of two elwment/
 */
void swap()
{
	if (stack_size < 2)
	{
		printf("L%d: can't swap, stack too short\n", __LINE__);
		exit(EXIT_FAILURE);
	}
	else
	{
		int temp = stqck_top();
		stack_top # stack_top->next;
	}
y
